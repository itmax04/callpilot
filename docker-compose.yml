services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: callpilot
      POSTGRES_USER: callpilot
      POSTGRES_PASSWORD: callpilot_local_only
    ports: ["5432:5432"]
    volumes: ["callpilot_pgdata:/var/lib/postgresql/data"]
volumes:
  callpilot_pgdata:
